# 데스크톱 실행·설치·설정 안정화 작업계획서

작성일: 2026-09-21 (Asia/Seoul)

상태: 구현 진행 중. 단계별 결과는 아래 진행 기록에 남긴다.

기준 배포: 0.1.6, 빌드 12. 구현 브랜치는 `origin/dev`의 `2a88f82`에서 새로 만든 `codex/desktop-reliability`다. [12 선제 점검](12_PROACTIVE_RELIABILITY_REVIEW.md)의 미배포 수정과 테스트 100개 결과는 별도 선행 작업이며, 아래 여섯 항목의 완료 증거로 사용하지 않는다. 기존 미커밋 변경을 보존하고 차이를 확인한 뒤 구현한다.

진행 방식: 서브에이전트 없이 순서대로 수행한다. 사용자가 구현 및 현재 환경 복구를 승인했다. 적절한 단계마다 커밋·푸시하며 PR은 생성하지 않는다. 운영 앱 교체와 공개 배포는 구현·격리 검증과 구분하고, 기존 0.1.6 릴리즈를 변경하지 않는다.

## 1. 목표와 확인된 근거

**같은 사용자의 자동 실행 주체를 하나로 제한하고, 설치본이나 저장 데이터가 바뀌어도 중복 전송·상태 소실·불필요한 권한 등록이 발생하지 않게 한다.**

| 대상 | 확인된 사실 | 해결 목표 |
| --- | --- | --- |
| 동시 실행 | 독립 `AppState` 두 개와 공유 저장소를 사용하는 격리 재현에서 워밍 2회, 저장된 완료 횟수 1회 | 두 프로세스가 동시에 시작해도 예약·인증 갱신·쓰기의 주체는 하나 |
| 설치본·구버전 | 같은 제품 ID를 쓰는 0.1.0·0.1.4·0.1.6 앱 복사본 존재 | 사용할 설치본을 명확히 하고 임시·구버전 복사본의 자동 실행 방지 |
| 설정값 검증 | 시간 `-15`, 요일 `[0, 8]`, 완료 횟수 `-2`가 읽기에서 통과. 다음 창 번호 `-1` 재현 | 생성자뿐 아니라 저장 데이터를 읽는 경계에서 검증 |
| 손상 복구 | 깨진 `dailyCycle` JSON을 읽으면 완료 횟수 0, 미확인 전송 없음으로 복귀 | 손상과 최초 실행을 구분하고 중요한 기록을 지운 채 재개하지 않음 |
| 버전 간 보호 | 명시적 스키마 버전과 미래 형식·구버전 실행에 대한 차단 없음 | 지원하는 형식만 읽고 쓰며 이전 과정은 중단·재시작에도 안전 |
| 잔여물 | 관련 macOS 앱 등록 13개 중 사라진 경로 6개. 테스트에서 사용하는 이름 패턴의 빈 plist 1,917개 | 개발·검증 과정에서 잔여물을 만들지 않고 확인된 기존 잔여물만 정리 |

복수 인스턴스 재현은 앱 로직을 격리해 실행한 결과다. 실제 제품 앱을 두 개 켜서 사용자 계정에 메시지를 전송한 검증은 아니다. 재현 입력과 출력은 `/tmp/claude-session-basics-audit.NzJ8lP`에 있으며, 핵심 결과는 위 표에 보존한다.

점검 당시 운영 설정값은 정상 범위였고, 실행 중인 제품 프로세스·로그인 항목·프로비저닝 프로필은 각각 하나였다. 빈 테스트 파일이 현재 장애의 원인이라는 증거는 없다. 일반적인 테스트 이름만으로 1,917개 전체의 소유권을 단정하거나 일괄 삭제하지 않는다.

## 2. 지켜야 할 동작 계약

1. 동일 macOS 사용자에게 제품 실행 소유자는 하나다. 다른 사용자 계정의 실행은 별개로 취급한다.
2. 실행 소유권과 저장소 준비 상태를 확인하기 전에는 `AppState`를 가동하거나, 인증 정보를 갱신하거나, 로그인 항목·설정을 쓰지 않는다.
3. 두 번째 실행은 기존 정상 설치본을 안내하거나 활성화한 뒤 종료한다. 사용자 모르게 다른 프로세스를 강제 종료하지 않는다.
4. 기본 운영 설치 위치는 현재 설치 방식에 맞춰 `/Applications/ClaudeSessionWarmer.app`로 정한다. DMG·Downloads·빌드 폴더의 복사본은 운영 예약과 로그인 항목 등록을 시작하지 않는다. 별도 설치 위치 선택 기능은 추가하지 않는다.
5. 정상 설정, 지원하는 구형 설정, 손상된 설정, 지원하지 않는 새 형식을 구별한다. 서로 같은 초기값 경로로 처리하지 않는다.
6. 손상이나 버전 불일치로 실행 기록을 신뢰할 수 없으면 자동 워밍을 멈춘다. 미확인 전송 표식이나 당일 완료 횟수를 임의로 지우지 않는다.
7. 사용자 설정·실행 기록·일시 캐시·인증 정보의 보존과 초기화 범위를 구분한다. 인증 정보는 기존 앱 전용 Keychain에만 둔다.
8. 정리 작업은 실행 중인 설치본, 운영 설정, 인증 정보, 다른 앱의 권한을 건드리지 않는다. 삭제 전에 대상과 이유를 제시하며 복구 가능하게 이동한다.

## 3. 구현 방향

### 3.1 실행 소유권과 설치본

- 사용자별 Application Support 디렉터리의 고정된 잠금 파일에 운영체제 파일 잠금(`flock`)을 사용한다. 파일 존재 여부나 PID 파일만으로 실행 여부를 판단하지 않는다.
- 잠금은 앱 수명 동안 유지하고, 자식 프로세스에 불필요하게 상속되지 않게 한다. 정상 종료·강제 종료 뒤에는 OS가 잠금을 해제하도록 한다. 실행 중 잠금 파일을 삭제해 서로 다른 파일에 잠금이 생기게 해서는 안 된다.
- 제품 진입점에서 설치 경로 확인 → 기존 실행 확인·소유권 획득 → 저장소 준비 → `AppState` 및 업데이트 기능 시작 순서로 구성한다. 현재 `startScheduler: false`만으로는 초기화 중 설정 쓰기까지 막히지 않으므로 이를 읽기 전용 모드로 간주하지 않는다.
- `LSMultipleInstancesProhibited` 같은 Launch Services 기능은 보조 수단으로 검토한다. 다른 사용자 세션에 미치는 영향과 실행 파일 직접 실행을 고려해 이것 하나만으로 완료 처리하지 않는다.
- 서로 다른 빌드에서 같은 사용자 잠금 경로를 사용한다. 시작 로그에 앱 경로·제품 버전·빌드·PID·실행 소유권 결과를 기록한다.

**구버전 한계:** 이미 배포된 0.1.6 이하에는 새 잠금을 따르는 코드가 없다. 새 버전의 스키마 번호나 잠금만으로 과거 바이너리를 소급해서 차단할 수 없다. 따라서 첫 전환에서는 실행 중인 구버전을 확인하고, 알려진 실행 가능한 복사본을 정리한 뒤 새 설치본으로 넘긴다. 구버전이 실행 중이면 새 버전은 상태 변경을 시작하지 않는다. 이후 구버전의 실행을 감지하면 새 버전의 후속 자동 작업을 멈추고 충돌을 알린다. 구버전을 사용자가 임의로 다시 가져와 실행하는 상황까지 완전한 상호 배제를 보장한다고 주장하지 않는다.

### 3.2 설정과 실행 기록 저장

현재 `SettingsStore`를 중심으로 읽기 결과와 검증을 명시한다. 별도 범용 저장소 프레임워크나 데이터베이스는 도입하지 않는다.

| 데이터 | 기본 저장 방향 | 비정상일 때 |
| --- | --- | --- |
| 예약 시각·요일·공휴일·사용자 선호 | 버전이 있는 UserDefaults 레코드 | 원본 보존, 수정·복구 안내, 자동 실행 정지 |
| 완료 횟수·실제 리셋·미확인 전송·재시도 | 사용자별 파일 하나에 버전이 있는 JSON으로 저장 | 원본 보존, 읽기 전용 복구 상태, 자동 재전송 금지 |
| 사용량 캐시 | 기존 캐시 유지 | 유효하지 않거나 만료되면 버리고 다시 조회 |
| OAuth·회전 토큰 | 기존 Data Protection Keychain 유지 | 기존 갱신·저장 복구 경로 사용. JSON·백업에 비밀 값 저장 금지 |

중요 실행 기록은 CLI 호출 전에 저장 완료를 확인해야 한다. `UserDefaults.set`은 메모리를 먼저 갱신하고 디스크에는 비동기로 기록하므로, 이를 전송 기록의 영속 저장 완료 응답으로 취급하지 않는다. 실행 기록 파일은 단일 소유자가 임시 파일 쓰기·동기화·원자적 교체를 수행하고, 실패하면 CLI를 시작하지 않는다. 마지막 정상본 백업은 하나만 보존한다. 앱 종료에 대한 복구와 OS·하드웨어의 모든 전원 손실 상황에 대한 보장은 구분한다.

읽기 결과는 최소한 `없음 / 정상 / 이전 필요 / 손상 / 지원하지 않는 버전`으로 구분한다. 사용자에게 보여 주는 상태는 기존 메뉴의 상태 표시와 복구 동작을 활용한다.

### 3.3 검증·이전·복구 규칙

- 예약 시각은 `0...1439`, 요일은 `1...7`의 비어 있지 않은 집합이어야 한다. 범위 밖 값을 예약 엔진에서만 잘라 쓰지 않는다. 잘못된 현재값은 사용자에게 수정 가능한 초안으로 보여 주고 저장 전에는 적용하지 않는다.
- 완료 횟수는 `0...3`, 재시도 횟수는 비음수이며 증가 연산이 넘치지 않아야 한다. 날짜는 유효한 유한 값이어야 하며, 날짜 키·리셋·재시도·전송 표식 간 관계도 검사한다.
- 구형 레코드는 해당 버전의 규칙으로 이전한 뒤 현재 형식의 관계 검증을 적용한다. 알려진 구형 필드 누락과 손상을 혼동하지 않는다.
- 현재의 버전 없는 레코드를 구형 형식으로 정의하고, 새 레코드에 `schemaVersion`과 최소 읽기 버전을 둔다. 제품 버전과 데이터 스키마 번호를 혼용하지 않는다.
- 새 실행 기록과 버전이 있는 설정은 구버전과 다른 파일·키에 저장한다. 구버전이 자신의 예전 키를 다시 써도 새 기록을 덮어쓰거나 재이전하지 않게 한다. 호환성을 위해 양쪽을 계속 동기화하는 방식은 사용하지 않는다.
- 이전 전 원본을 보존하고 새 기록을 쓴 뒤 재읽기 검증한다. 성공 이후에만 이전 완료를 확정한다. 단계 중간에 종료돼도 다음 실행에서 같은 결과로 이어져야 한다.
- 현재 앱이 읽을 수 없는 미래 형식은 수정·초기화하지 않는다. 맞는 버전으로 업데이트하도록 안내하고 해당 상태에 의존하는 자동·수동 워밍을 막는다.
- 손상된 실행 기록을 오래된 정상 백업으로 되돌렸다고 곧바로 워밍하지 않는다. 백업에 마지막 전송이 없을 수 있기 때문이다. 서버 상태 확인과 기존 미확인 전송 정책을 적용하고, 결과가 여전히 불명확하면 명시적인 재전송 판단 전까지 정지한다.
- 공장 초기화와 캐시 비우기를 같은 동작으로 만들지 않는다. 캐시 비우기는 예약·전송 기록·인증에 영향을 주지 않아야 한다. 실행 기록 초기화는 중복 전송 위험을 설명하고 별도로 처리한다.

## 4. 작업 순서

| 단계 | 작업 | 주요 대상 | 완료 기준 |
| --- | --- | --- | --- |
| T0 | 현재 상태와 선행 변경 고정 | Git diff, 12 점검 기록, 설치본·등록 항목 목록 | 배포 0.1.6과 미배포 변경을 구분하고 운영 설정의 비밀 값 없는 보존본 확보 |
| T1 | 실행 소유권 및 시작 순서 | `ClaudeSessionWarmerApp.swift`, 작은 실행 소유권 모듈, `AppState.swift` | 같은 사용자에서 소유자 하나. 두 번째 프로세스의 인증·전송·상태 쓰기 0회 |
| T2 | 설치·업데이트 경로 정리 | 앱 진입점, `AppUpdater.swift`, `packaging`, 배포 스크립트 | 임시 위치에서는 운영 작업을 시작하지 않음. 업데이트는 실제 운영 설치본을 대상으로 수행 |
| T3 | 검증 및 중요한 기록 저장 | `Models.swift`, `SettingsStore.swift`, 필요 시 작은 실행 기록 파일 저장 모듈 | 범위·관계 검사, 저장 오류 전달, 저장 완료 전 전송 금지 |
| T4 | 구형 데이터 이전·손상 복구·버전 차단 | 저장소, `AppState.swift`, `MenuContent.swift` | 구형 데이터 보존·이전 재진입, 손상 시 정지, 미래 버전 파일 무변경 |
| T5 | 개발·테스트 격리와 잔여물 정리 | 테스트 fixture, 검증 스크립트, 별도 정리 도구 | 새 테스트 잔여물 생성 0건. 확정된 기존 잔여물은 목록에 따라 복구 가능하게 정리 |
| T6 | 통합 검증과 인계 | 아래 검증표, 관련 사용자 문서 | 새 프로세스·설치·데이터 계약까지 검증하고 설치·배포 여부를 별도 기록 |

T1의 시작 차단과 T3·T4의 저장소 준비 판정이 모두 적용되기 전에는 운영 데이터로 전환하지 않는다. 기존 앱을 정상 종료하고 새 설치본을 가동하는 순서는 설치 단계에서 다룬다. 첫 전환에서 구버전의 인증 갱신·워밍을 강제로 끊지 않는다.

로그인 항목은 운영 설치본만 등록할 수 있게 한다. OS에서 사용자가 끈 상태를 예약 시각 변경 등 다른 설정 저장으로 다시 켜지 않도록, 실제 상태와 사용자가 이번에 바꾼 항목을 구분한다.

## 5. 잔여물 정리 범위

정리는 제품의 매 실행 시 광범위하게 수행하지 않는다. 먼저 발생을 막고, 이미 남은 항목은 별도 일회성 정리로 처리한다.

1. 앱 복사본·설정 파일·macOS 등록 경로를 읽기 전용으로 목록화한다. 앱 ID, 경로, 버전·빌드, 존재 여부, 실행 여부, 정리 근거를 기록한다.
2. 운영 설치본과 현재 작업 중인 산출물을 제외한다. 알려진 구버전·검증 복사본은 실행하지 않는 것을 확인한 뒤 보관 또는 휴지통으로 이동한다. 이름이 비슷하다는 이유로 다른 앱을 제거하지 않는다.
3. 사라진 경로의 앱 등록은 해당 경로 단위로만 해제한다. 전체 Launch Services, 로그인 항목, TCC 권한 데이터베이스를 초기화하지 않는다. OS가 개별 해제를 허용하지 않은 항목은 성공으로 기록하지 않는다.
4. 테스트 설정은 프로젝트 귀속과 빈 내용을 확인한 항목만 대상으로 한다. 테스트가 실행 중이지 않은 상태에서 지원되는 defaults API·명령으로 도메인을 정리하고, 남은 빈 파일은 별도 목록으로 확인한다. 접두사만 일치하는 귀속 불명 항목은 제외한다.
5. 단위 테스트는 메모리 저장소를 사용하고, 파일 검증은 전용 임시 디렉터리를 사용한다. 실제 제품 서명·설치·Keychain 검증은 격리된 macOS 사용자 또는 검증 환경에서 수행한다. 제품 ID가 필요한 검증에서 단순히 다른 ID로 바꿔 검증 의미를 잃지 않도록 한다.
6. 정리 전후 목록과 제외 사유를 남긴다. 보존한 정상 설정, 운영 프로필·인증서·Keychain, 실행 예약이 바뀌지 않았는지 확인한다.

현재 하나인 운영 프로비저닝 프로필과 기존 Developer ID·공증 설정은 재사용한다. 일반 사용자가 앱 사용을 위해 개발용 프로필을 직접 설치하도록 요구하지 않는다. 운영 잠금 파일과 정상 상태·복구 백업은 필요한 파일이며 빈 테스트 파일과 함께 정리하지 않는다.

## 6. 검증표

검증은 실제 계정에 중복 메시지를 보내지 않는 가짜 서비스와 격리 저장소를 기본으로 한다. 운영체제 수준 검증은 별도로 수행한다.

| 항목 | 조건 | 통과 기준 |
| --- | --- | --- |
| 동시 시작 | 같은 사용자에서 두 프로세스를 동시에 실행 | 소유권 획득 1개, 워밍 1회, 완료 기록 1회 |
| 다른 경로의 복사본 | Applications·Downloads·DMG·빌드 폴더 | 운영 설치본만 일정·인증·로그인 항목을 변경 |
| 직접 실행 | 앱 실행 파일을 직접 두 번 실행 | Launch Services를 거치지 않아도 중복 작업 방지 |
| 강제 종료 | 소유자 종료 후 재실행 | 잠금 파일이 남아도 소유권 재획득. 미확인 전송 표식은 보존 |
| 구버전이 먼저 실행됨 | 0.1.6 이하가 켜진 상태에서 새 버전 시작 | 새 버전의 인증·워밍·이전 쓰기 0회, 전환 안내 |
| 사용자 분리 | 서로 다른 macOS 사용자 | 사용자별 소유권과 설정이 독립적임 |
| 잘못된 값 | 음수·범위 초과·비정상 요일·정수 경계·불일치 날짜 | 음수 창 번호·무의미한 예약·연산 오버플로 없음 |
| 레코드 손상 | 잘린 JSON, 잘못된 타입, 미확인 전송 레코드 손상 | 원본 보존, 이유 표시, 무조건 0으로 초기화하거나 전송하지 않음 |
| 기록 저장 실패 | 읽기 전용·디스크 쓰기 실패·원자적 교체 실패 | 성공으로 표시하지 않음. 전송 전 저장 실패면 CLI 시작 0회 |
| 이전 중단 | 이전 단계마다 종료 후 다시 시작 | 기존값 소실·중복 이전 없음, 완료 검증 전 원본 제거 없음 |
| 미래 형식 | 현재 앱이 모르는 스키마 | 데이터 파일과 구형 키 무변경, 자동·수동 전송 차단 |
| 다운그레이드 | 이전 후 구버전 실행·쓰기 | 구형 형식으로 새 기록이 덮어써지지 않음. 구버전의 잠금 미지원 한계도 기록 |
| 설정별 초기화 | 캐시 삭제와 실행 기록 복구를 각각 수행 | 캐시 삭제가 인증·예약·미확인 전송에 영향 없음 |
| 로그인 항목 변경 | OS에서 끈 뒤 앱에서 예약 시각만 저장 | 사용자가 끈 로그인 항목을 다시 등록하지 않음 |
| 테스트 재실행 | 동일 검증을 반복 | 운영 설정·권한·로그 무변경, 새 빈 Preferences 파일 및 시험 앱 등록 잔여 없음 |
| 새 환경 설치 | 수동 개발 프로필 설치 이력이 없는 환경 | 서명·공증된 앱의 정상 실행 및 앱 전용 저장소 접근 확인 |
| 기존 무인 흐름 | 지연·재시작·전송 전 실패·미확인 전송·사용량 창 상한 | 12 점검에서 확보한 복구와 중복 방지 동작 유지 |

기본 검증은 `swift test`, `swift build -c release`, 기존 실제 타이머·격리 Keychain 검증을 사용한다. 필요한 다중 프로세스·파일 손상 검증만 추가한다. 두 `AppState` 객체의 모델 재현만으로 macOS의 동시 실행 방지가 완료됐다고 판단하지 않는다.

## 7. 결과물과 완료 기준

- [ ] 실행 소유권이 저장소·인증·예약보다 먼저 확보됨
- [ ] 임시·구버전 복사본과 운영 설치본의 행동이 구분됨
- [ ] 읽기 검증, 스키마 이전, 손상·미지원 버전의 정지 동작 구현
- [ ] 중요한 실행 기록의 저장 실패가 전송 차단으로 이어짐
- [ ] 미확인 전송·완료 횟수·기존 인증이 초기화로 소실되지 않음
- [ ] 이전 중단·재시작·복구·정상 업데이트 경계의 검증 통과
- [ ] 테스트가 사용자 환경에 새 잔여물을 만들지 않음
- [ ] 확정된 정리 대상과 제외된 항목의 전후 목록 보존
- [ ] 사용자 문서와 검증 결과에 지원 범위·구버전 한계·남은 조건 반영

코드 구현, 격리 검증, 운영 설치본 교체, 실제 잠금 중 예약 성공, 공개 배포를 각각 구분해 보고한다. 배포를 진행할 때는 기존 dev → main PR 흐름과 서명·공증 설정을 재사용한다. 새 버전·빌드는 배포 단계에서 정하며 이미 공개한 0.1.6 파일을 덮어쓰지 않는다.

새 서버·상주 helper·주기적인 전체 디스크 탐색·전역 OS 설정 초기화·새 인증 체계는 추가하지 않는다. 실제 잠자기 조작이나 사용자 계정으로의 반복 워밍도 이 계획의 기본 검증 수단으로 사용하지 않는다.

## 근거

- [앱 진입점](Sources/ClaudeSessionWarmer/ClaudeSessionWarmerApp.swift), [상태와 시작 순서](Sources/ClaudeSessionWarmer/AppState.swift)
- [설정 저장소](Sources/ClaudeSessionWarmer/SettingsStore.swift), [저장 모델](Sources/ClaudeSessionWarmer/Models.swift)
- [선행 선제 점검](12_PROACTIVE_RELIABILITY_REVIEW.md), [Keychain 전환 기록](11_UNATTENDED_KEYCHAIN_FIX.md)
- [Apple UserDefaults](https://developer.apple.com/documentation/foundation/userdefaults): 메모리 즉시 갱신과 비동기 디스크 기록, 설정 파일 직접 수정 시 주의
- [Apple 다중 사용자·다중 실행 설명](https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPMultipleUsers/Concepts/FastUserSwitching.html): Launch Services의 다중 실행 제한 범위

## 진행 기록

- T0: 최신 `origin/dev`를 조회하고 같은 커밋에서 새 브랜치 생성. 기존 미배포 수정은 별도 선행 커밋으로 보존. 운영 설정의 `scheduleSettings`, `dailyCycle`, `quotaCache`만 `.build/reliability-baseline/operational-preferences.plist`에 백업했다. 인증 비밀 값은 포함하지 않았다. 이 경로는 Git에 올리지 않는다.
- 선행 변경 검증: 이전 점검의 테스트 100개 및 release 빌드 결과는 12 문서에 기록되어 있다. 이후 회귀 검증은 테스트 설정 격리를 적용한 뒤 재실행한다.
- T5 선행 격리: 단위 테스트와 실제 타이머 검증의 UserDefaults를 메모리 저장소로 교체. `swift test` 100개 통과. Preferences 1,917개에 신규·내용 변경 0건. 실제 타이머 지연 147초 재현에서 조회 2회, 가짜 워밍 1회, 성공 기록 확인. 기존 잔여물 정리는 아직 수행하지 않았다.
