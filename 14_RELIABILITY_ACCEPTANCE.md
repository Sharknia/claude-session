# 데스크톱 안정화 검증 결과

기준: 2026-09-21, `codex/desktop-reliability`. 공개 릴리즈와 운영 설치본은 0.1.6/빌드 12를 유지한다. PR은 생성하지 않았다. 아래 결과는 [13 작업계획서](13_DESKTOP_RELIABILITY_PLAN.md)의 구현·현재 환경 복구·검증 범위를 구분한다.

## 구현 및 격리 검증

| 계획의 검증 항목 | 결과 | 확인한 근거 또는 남은 조건 |
| --- | --- | --- |
| 같은 사용자 동시 시작 | 통과 | `verify-execution.py` 10회, `verify-concurrent-storage.py` 3회. 실제 프로세스에서 소유자/가짜 워밍/완료 기록 각각 1개 |
| 다른 경로의 복사본 | 코드·격리 검사 통과 | canonical 경로 검사와 임시 경로의 업데이트/로그인 등록 차단. 실제 설치본 전환은 미수행 |
| 실행 파일 직접 실행 | 통과 | 별도 경로에 복사한 실행 파일도 동일 잠금으로 차단 |
| 강제 종료 | 통과 | SIGKILL 후 잠금 재획득. 실제 상태 파일의 전송 표식 유지, 재실행 시 가짜 요청 0회 |
| 구버전이 먼저 실행됨 | 구성 요소 검사 통과 | 실제 가짜 구버전 bundle을 직접 실행해 탐지. 차단 상태에서 인증·쓰기·전송 0회. 운영 설치본을 교체한 검증은 미수행 |
| 서로 다른 macOS 사용자 | 미검증 | 별도 디렉터리의 잠금 독립성은 확인했으나 실제 다른 사용자 세션으로 실행하지 않음 |
| 잘못된 설정값·날짜·정수 경계 | 통과 | 저장 경계의 범위·관계 검사, Int.max 재시도 증가 시 오버플로 없음 |
| 레코드 손상 | 통과 | 잘린 JSON·잘못된 타입을 손상으로 구분, 원본 무변경 및 실행 차단 |
| 기록 저장 실패 | 통과 | 실제 읽기 전용 디렉터리와 원자적 교체 실패. 전송 전 저장 실패 시 호출 0회, 전송 후 실패 시 표식 유지 |
| 이전 중단 | 통과 | 파일 쓰기 각 단계 실패 후 재진입. 이전 도중 구형 키가 바뀌어도 최초 보존본에서 이어짐 |
| 미래 형식 | 통과 | 시작 및 실행 중 형식 교체를 감지. 인증·워밍·초기화 차단, 미래 백업도 덮어쓰지 않음 |
| 다운그레이드 데이터 보호 | 통과, 한계 명시 | 구형 키를 다시 써도 새 상태 파일에 반영하지 않음. 이미 배포된 구버전 자체의 실행을 소급 차단하는 기능은 아님 |
| 캐시/실행 기록 복구 분리 | 통과 | 캐시 비우기는 설정·실행 기록을 유지. 실행 기록 복구는 원본을 보존하고 당일 자동 워밍 중지 및 미확인 표식 유지 |
| OS에서 로그인 실행을 끈 뒤 일정 저장 | 코드 경계 확인 | 로그인 토글을 명시적으로 편집했을 때만 등록 변경. 실제 OS 토글과 새 운영 설치본의 연동은 미검증 |
| 반복 테스트와 잔여물 | 로컬 검사 통과 | 메모리 설정 사용, 신규 Preferences 생성 없음. 실제 메뉴 검증 앱 종료 뒤 등록 및 임시 디렉터리 0개 |
| 새 환경의 서명된 앱 설치·Keychain | 미검증 | 현재 계정에는 개발 프로필 설치 이력이 있음. 사용 가능한 별도 Mac/테스트 계정을 질문한 상태 |
| 기존 무인 실행 흐름 | 통과 | 전체 테스트 120개 및 실제 타이머 지연 147초에서 조회 2회/가짜 워밍 1회 |

주요 재현은 [StorageReliabilityTests](Tests/ClaudeSessionWarmerTests/StorageReliabilityTests.swift), [ExecutionOwnershipTests](Tests/ClaudeSessionWarmerTests/ExecutionOwnershipTests.swift), [동시 프로세스 검증](scripts/verify-concurrent-storage.py)에 남겼다. `swift build -c release`도 통과했다. 검증용 실행 파일도 제품의 최소 지원 버전과 같은 macOS 14를 대상으로 컴파일하도록 맞췄다. 테스트에서 실제 Claude 요청이나 운영 인증 갱신은 수행하지 않았다.

## 메뉴 동작

실제 `MenuContent`를 제품과 다른 ID의 임시 앱에서 실행했다. 저장소·조회·로그인·워밍은 모두 주입한 메모리/가짜 서비스를 사용했다. 검증용 창이므로 운영 메뉴바 진입점과 실제 Sparkle 설치 동작의 검증으로 간주하지 않는다.

- 손상 설정: 원인 안내와 편집용 초안 확인 → 저장 클릭 → `저장됨`, 차단 해제, 기존 완료 횟수 2회 유지.
- 손상 실행 기록: 복구 확인 창에서 취소 → 원래 차단 유지. 다시 열어 확정 → `확인 필요`, `오늘 자동 워밍 중지`, 다음 날 일정과 미확인 전송 버튼 확인.
- 가짜 로그인 및 상태 확인 후에도 미확인 표식과 당일 중지 상태 유지. UI에서도 재전송하지 않는 이유를 표시.
- 미지원 형식: 업데이트 필요 안내, 로그인·저장·워밍 비활성화, 덮어쓰는 복구 동작 없음.
- 불확실한 완료 횟수의 진행 막대가 채워져 보이던 것을 수정하고 실제 화면에서 빈 막대 및 중지 표시 확인.

재현: `bash scripts/preview-recovery-menu.sh settings`, `runtime`, `future`. 검증 앱의 종료 버튼을 누르면 임시 bundle을 삭제한 뒤 해당 경로의 앱 등록을 해제한다. `/var` 별칭과 실제 `/private/var` 경로를 섞지 않도록 물리 경로를 사용한다. 경로 정규화만 적용했을 때 등록이 남는 사례가 있었고, 삭제 후 해제 순서를 적용한 실제 실행·종료에서 잔여 0개를 확인했다. 이 결과는 과거 OS 권한 화면 잔여물 전체의 원인을 입증하는 것은 아니다.

## 현재 환경 복구

- 구버전 0.1.0·0.1.4 앱 2개와 완료된 업데이트 검증 앱 2개를 휴지통으로 이동.
- 임시 검증 방식으로 대체한 구형 VerifyWarmup 앱 1개도 별도 목록·해시 검토 후 휴지통으로 이동. 이 경로의 등록도 제거 확인. 전체 보관 정리는 앱 5개와 관련 등록 12개.
- 프로젝트 전용 빈 SleepProbe 설정 7개를 원본 백업 후 정리.
- 선택한 앱 등록 5개(포함된 Sparkle helper 1개 포함)와 사라진 경로 등록 6개 제거 확인.
- 일반적인 테스트 접두사의 빈 설정 1,910개는 개별 프로젝트 귀속을 확인할 수 없어 제외. 현재 작업 경로의 dist 앱은 배포 0.1.6 보존 산출물로 유지.
- 정리 직후 운영 앱 PID 33526, 0.1.6/빌드 12, 예약·실행 기록·캐시 값, 로그인 항목 enabled/allowed/notified(세대 14), 운영 프로필 96fa1ff5-ef49-422d-bc52-426d2d4472b3 유지 확인.

복구 목록·파일 해시·설정 원본·휴지통 위치는 Git에 포함하지 않은 `.build/reliability-baseline/local-cleanup-manifest.json` 및 `.build/reliability-baseline/recovery-20260921-142508/`에 보존했다. 메뉴 검증 후 최종 잔여물과 운영 설정 확인은 `.build/reliability-baseline/menu-final-verification.json`에 기록했다.

추가 검증에서 분리되어 남아 있던 메뉴 검증 프로세스 6개도 정확한 실행 파일 경로로 종료했다. 메뉴 검증 앱은 시나리오 인수가 없으면 UI 생성 전에 종료하도록 바꿨고, 인수 없는 직접 실행에서 종료 코드 2를 확인했다. 최종 점검에서 메뉴 검증 프로세스·폴더·등록 모두 0개였다. 계정 검증 도구도 인수 없음/잘못된 인수에서는 계정 접근 전에 종료하며, 실제 워밍은 `--warmup`을 명시해야 한다. 실제 계정 모드에는 제품 실행 잠금과 구버전 검사도 적용했다. 기존 Keychain 검증 앱의 추가 보관 기록은 `.build/reliability-baseline/recovery-20260921-150245/`에 있다.

운영 기록은 정리 후에도 정상적으로 진행됐다. 설치된 0.1.6의 14:50 예약은 14:50:39에 워밍 성공·완료 횟수 2회를 기록했고 다음 예약을 19:50으로 설정했다. 따라서 이후 실행 기록이 최초 보존본과 달라진 것은 이 예약 진행 때문이며, 예약 설정은 그대로였다. 이는 현재 설치된 0.1.6의 운영 결과이며 미배포 브랜치나 잠금 상태 검증의 증거로 사용하지 않는다.

이전 메타데이터만 손상된 경우에는 원본을 보존하고 검증된 현재 설정·실행 기록을 그대로 유지해 복구하도록 보완했다. 이 회귀 검사까지 포함해 전체 테스트 120개와 release 빌드가 통과했다.

## 아직 완료로 표시할 수 없는 부분

실제 다른 사용자 세션, 수동 개발 프로필 설치 이력이 없는 환경의 서명/Keychain 동작, 새 설치본의 실제 로그인 항목·업데이트 전환은 증거가 없다. 현재 계정의 프로필을 삭제하거나 운영 앱을 시험용으로 교체해서 이 조건을 만들지 않았다. 사용할 환경이 확인되면 기존 서명·공증 설정을 재사용해 해당 검증을 이어간다.

운영 앱 교체·실제 잠금 중 예약 성공·새 공개 배포는 별도 단계다. PR 금지 요청을 유지하고 있으며 공개된 0.1.6 산출물을 덮어쓰지 않았다.
