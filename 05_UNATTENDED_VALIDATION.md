# 2026-09-08 무인 실행 검증

상태: 사전 자동 테스트 완료, 실제 잠금 상태 무인 예약 검증 대기.

## 사전 확인

- 자동 테스트 63개 통과. 가짜 서비스 응답을 사용하는 예약 처리 → 상태 저장 → 앱 상태 재생성 → 후속 예약 실행, 중복 콜백 무시, 타임아웃 후 중복 전송 방지, 인증 거절 시 호출 0회를 포함한다. 이것을 실제 잠금 또는 서버 통합 검증으로 간주하지 않는다.
- 실제 앱 Keychain의 비대화형 읽기 성공. 확인 시 access token 만료는 2026-09-07 18:13:17 KST. 이후 앱에서 추가 갱신이 일어날 수 있으므로 내일 실제 로그의 `previous_expires_at`으로 다시 판정한다.
- 실제 설정은 첫 시각 06:00, 월~금, 한국 공휴일 제외, 로그인 시 실행 켜짐이다. 9월 8일은 실행 대상이다.
- 현재 AC 전원 연결, AC 시스템 잠자기 0. 배터리 전원 잠자기는 1이므로 야간에는 전원 연결과 덮개 열림을 유지해야 한다. 시스템 전원 설정은 변경하지 않았다.
- 검증용 앱 0.1.0 빌드 2, Developer ID Application: HakKyeol Lee (V9SQZ6B7RP) 서명. 공증은 별도 준비 중이며 공개 배포본은 아니다. `app.started.app_build`에 빌드를 기록한다. 최종 배포 합격 표시는 실제 검증 이후에 한다.
- 오늘 14:41 능동 검증은 내일 06:04 수동 조작 없는 로그 관찰로 변경했다. 사용자는 오늘 Claude 사용을 조절할 필요가 없다.

## 실제 검증

사용자는 업무 종료 후 Mac을 전원에 연결하고 덮개를 열어 둔 채 화면을 잠근다. 앱은 실행 상태로 유지한다. 로그인·수동 워밍 버튼은 검사 목적으로 누르지 않는다. 검증을 위해 계정 로그아웃이나 토큰 수정은 하지 않는다.

앱 자체의 2026-09-08 06:00 예약이 실행 주체다. 후속 자동화는 06:04 이후 로그를 읽고 판정할 뿐, 대신 워밍하거나 인증을 갱신하지 않는다.

`~/Library/Logs/ClaudeSessionWarmer/events.jsonl`과 이전 파일에서 `target_at=2026-09-07T21:00:00.000Z`를 찾고 아래를 같은 launch 및 operation으로 연결한다.

1. 빌드 2의 시작 기록과 화면 잠금 이벤트. 실행 완료까지 잠금 해제 및 시스템 잠자기가 없는지 확인한다. 이벤트가 부족하면 잠금 검증은 미확인이다.
2. 06:00 `timer.fired`와 지연 시간.
3. 이미 만료된 `previous_expires_at` → `oauth.refresh_http` 200 → `oauth.refresh_completed` 성공. 완료 로그는 Keychain 저장 이후 기록된다. 토큰이 만료되지 않았다면 만료 갱신 조건은 미검증이다.
4. `quota.decision`의 `scheduled_pre`가 비활성. 이미 활성 상태라면 `already_active`는 정상 동작이지만 새 창 생성 합격은 아니다.
5. `warmup.requested` scheduled → `warmup.process_started` → `warmup.process_finished` marker_received. 중복 호출이 없어야 한다.
6. `scheduled_post` 활성 → `window.completed` warmed → 실제 리셋 시각을 향한 다음 예약. 실제 리셋이 호출 시각 + 5시간에서 크게 벗어나면 성공으로 단정하지 않고 원인을 조사한다.
7. 실제 다음 리셋 이후에 후속 예약이 처리됐는지 확인한다. 06:04에 다음 예약이 잡혔다는 사실만으로 두 번째 실행까지 통과했다고 보고하지 않는다.

수동 실행, 재로그인, 미확인 잠금 상태로 결과를 보완하지 않는다. 실패 시 해당 원인과 로그를 보존한다. 첫 실행 성공이면 후속 관찰 시각을 실제 리셋 직후로 조정하고, 전체 통과 또는 실패 판단 후 자동화를 중지한다.

## 남은 범위

실제 화면 잠금 중 만료 토큰 갱신·예약 워밍·비활성 창 활성화·후속 예약 실행은 아직 확인하지 않았다. 사전 테스트나 CLI 단독 성공으로 대체할 수 없다. 검증용 실행 파일과 DMG의 SHA-256은 `.build/preflight/artifact-hashes.txt`에 보존한다.
