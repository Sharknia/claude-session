# 무인 실행 선제 점검

2026-09-21. 기준: 배포된 0.1.6(빌드 12), `main`의 `6a880b4`와 동일한 앱 소스.

작업 브랜치: `codex/prevent-unattended-failures`. 아래 수정은 로컬 검증을 마쳤으며 아직 배포하거나 설치하지 않았다. 사용자가 설치한 0.1.6의 실행·설정·실제 계정 인증 정보는 변경하지 않았다.

## 왜 테스트가 통과했는데 다른 오류가 이어졌는가

기존 검증은 정상 실행과 이미 알려진 장애를 중심으로 구성됐다. 특히 `testFailuresRemainPendingAndRetriesAreBoundedAcrossRestart`는 네 번째 서버 오류 뒤 당일 처리를 포기하는 동작을 정상으로 검사했다. Keychain만 느린 재시도를 추가한 0.1.6에서는 같은 문제가 네트워크·서버 오류로 발생할 수 있었다.

무인 앱의 검증 기준은 오류 이름보다 결과에 있어야 한다. 일시 장애가 해소되면 수동 조작 없이 재개하고, 확실히 전송하지 못한 요청은 재시도할 수 있어야 한다. 반대로 이미 전송했을 수 있는 요청은 중복 전송하지 않아야 한다. 외부 명령이 멈추거나 인증 갱신 중 업데이트가 시작돼도 앱의 상태가 망가지지 않아야 한다.

## 발견과 조치

| 조건 | 0.1.6의 결과 | 이번 수정과 검증 |
| --- | --- | --- |
| 사용량 조회·OAuth 서비스 오류·429가 네 번 연속 발생 | 그날 예약을 닫고 다음 날로 이동 | 짧은 재시도 후 5분 간격으로 당일 대상 유지. 서비스 복구와 재시작 뒤 1회 워밍 확인 |
| 비활성 확인 후, CLI 실행 직전 인증 정보 읽기 실패 | 실제 전송 없이도 미확인 전송 표식이 남아 이후 워밍 차단 | 프로세스 시작 전 오류만 표식 해제. 재시작·인증 복구 후 1회 워밍 확인. 전송 후 타임아웃의 중복 방지는 유지 |
| 메뉴의 백그라운드 조회가 토큰을 갱신하는 중 업데이트 재시작 요청 | 예약 워밍 중인 경우만 기다리므로 백그라운드 갱신은 중단 가능 | 백그라운드 조회도 진행 중인 작업으로 처리. 완료 후 설치 콜백 1회 재개 확인 |
| CLI 경로를 찾는 로그인 셸이 멈추거나 stderr가 가득 참 | stdout EOF와 프로세스 종료를 무제한 대기 | 경로 조회 5초 제한, stderr 폐기, stdout 크기 제한. 실제 가짜 프로세스로 시간 제한·대량 stderr 회귀 검증 |
| 시작할 때 이전 사용량 캐시는 있지만 실제 CLI·인증이 잘못됨 | 예약 도래 전까지 실제 확인 없이 연결 상태로 보일 수 있음 | 예약까지 30초 이상 남으면 시작 시 실제 준비 상태 확인. 워밍은 실행하지 않으며 예약·당일 횟수 유지 |

첫 세 항목은 수정 전 테스트 3개에서 재현됐다(11개 assertion 실패). 백그라운드 조회를 실제 계정에 연결하지 않도록 기존 주입 콜백과 연결하는 테스트 경로만 먼저 정리했고 기본 서비스 구현은 유지했다. CLI 경로 조회는 배포 버전의 함수를 별도 프로그램으로 추출해 stderr 파이프가 가득 차면 완료되지 않는 것을 확인한 뒤 격리 프로세스 그룹을 종료했다.

CLI 응답 대기와 프로세스 종료 제한은 시스템 시각 변경에 영향을 받지 않는 `ContinuousClock`을 사용한다. 네트워크 실패 로그에는 비밀 값이나 응답 본문 대신 원래 오류 domain·code만 추가한다.

## 반복 방지 검사

`scripts/build-dmg.sh`는 배포 파일 생성 전에 전체 테스트와 `verify-warmup.sh --storage-probe`를 실행한다. 실패하면 기존 배포 파일을 지우거나 새 패키지를 만들기 전에 중단한다. 후자는 실제 계정과 분리한 임시 Keychain 항목으로 이전·갱신·접근 정책을 검증한다. 검증 도구의 로그도 운영 로그와 분리했다.

| 검사 | 이번 결과 |
| --- | --- |
| 전체 XCTest | 100개, 실패 0 |
| Release 컴파일 | 성공 |
| 실제 임시 Keychain 이전·갱신·정리 | 성공, AfterFirstUnlockThisDeviceOnly 실제 반환값 확인 |
| 검증 로그 분리 | 임시 로그의 launch ID가 운영 로그에 없음을 확인 |
| 설치된 Claude CLI 호환성 | 2.1.258, 현재 사용하는 옵션을 포함한 `--help` 실행 성공. 실제 메시지는 전송하지 않음 |
| 실제 타이머 147초 지연 | 가짜 서비스로 지연 복구 검증. 실제 잠자기 조작 없음 |

증거는 `/tmp/claude-session-preventive-repro.log`, `/tmp/claude-session-preventive-tests.log`, `/tmp/claude-session-preventive-build.log`, `/tmp/claude-session-preventive-keychain.log`, `/tmp/claude-session-preventive-scheduler.log`에 있다. 임시 파일이므로 핵심 판정을 이 문서에도 보존한다.

## 계속 구분해야 하는 한계

- 서버 장애나 인터넷 단절 자체를 막을 수는 없다. 이번 수정은 회복됐을 때 앱이 계속 포기한 상태로 남지 않도록 한다.
- Claude CLI 옵션·출력 형식이나 비공개 사용량 API가 바뀌면 호환성 수정이 필요하다. 현재 버전 확인은 향후 변경의 보장이 아니다. 잘못된 응답으로 활성화를 추정하거나 무조건 재전송하지 않는다.
- 이미 전송했을 수 있지만 서버에서 결과를 확인하지 못하는 경우에는 자동 재전송을 계속 막는다. 이 정책을 시간 경과만으로 풀지는 않았다.
- OAuth가 토큰을 회전한 뒤 저장하지 못한 채 프로세스가 종료되면 재로그인이 필요할 수 있다. 진행 중인 정상 업데이트 재시작은 막았지만, 저장 실패 후 메모리에만 남은 값이나 강제 종료까지 영구 보존한 것은 아니다.
- 서로 다른 앱 복사본의 동시 실행을 배제하는 전역 잠금은 없다. 현재 회귀 검증은 한 앱 프로세스 안의 중복 요청과 재시작을 다룬다. 여러 복사본 간 인증 갱신·전송 중복은 별도 검증 대상이다.

이 검증은 0.1.6에서 오늘 이 모든 오류가 발생했다는 주장이 아니다. 코드에서 예측한 조건을 재현해 예방한 범위이며, 실제 잠금 상태의 장시간 실행과 외부 서비스의 모든 변화를 검증했다고 주장하지 않는다.
