# 예약 타이머 잠자기 누적 지연 수정 계획서

작성일: 2026-09-14 (Asia/Seoul)

상태: TASK-01~03 구현, 자동 회귀, 서명·공증 및 0.1.4 설치 완료. 오늘·내일 이후 실사용 예약 검증 진행.

추가 사용자 요청: 이 브랜치의 제품 버전은 0.1.4로 고정한다. 버전 표시와 수동 업데이트 확인·원클릭 설치 기능을 추가했으며 상세 내용은 [08_UPDATER.md](/Users/crobat/dev/claude-session/08_UPDATER.md)에 기록한다. 최신 설치는 빌드 9이며, Keychain 프로필 접근 복구 후 이 빌드의 공증과 설치 앱의 Gatekeeper 검증도 완료했다.

검증 범위 변경(2026-09-14 사용자 지시): 강제로 잠자기를 반복하는 검증은 수행하지 않는다. 오늘 실사용과 내일 이후 자연스러운 예약 실행으로 실기기 검증을 대체한다. 자연스러운 잠자기 유무는 관찰 사실로 남기고, 잠자기 기록이 없는 시점의 오류도 조사한다.

기준 코드: `main`, `fe1a3c6431004b31fdff4210a86874c76a1fed2b`

장애 관찰 설치본: 0.1.3, 빌드 5

진행 방식: 서브에이전트 없이 단일 에이전트가 순서대로 진행한다. 2026-09-14 구현 요청에 따라 최신 `origin/main`에서 `codex/fix-wall-clock-scheduling`을 생성했으며, 단계별 커밋·푸시를 수행한다. 구현과 실기기 검증의 완료 여부를 별도로 기록한다.

## 1. 목표와 원인

예약을 설정한 뒤 Mac이 잠자기에 들어갔다가 예정 시각 전에 깨어나도, 예약 시각이 잠든 시간만큼 뒤로 밀리지 않도록 수정한다.

수정 전 0.1.3 구현은 목표 시각까지 남은 시간을 한 번 계산해 `Timer.scheduledTimer`에 넣었다. 이 타이머가 사용하는 시간 기준은 시스템 잠자기 동안 진행하지 않는다. 복귀 시 기존 예약을 다시 계산하는 처리도 없어 실제 시각과 타이머가 어긋났다.

예를 들어 11시에 16시 실행을 예약하고 중간에 약 1시간 잠들면, 깨어난 뒤에도 기존 카운트다운을 유지해 17시경 처리될 수 있다. 16시 당시 Mac과 앱이 정상 실행 중이었다는 사실만으로 이 문제를 배제할 수 없다.

### 장애 근거

동일 프로세스의 예약 처리 지연과, 별도 큐에서 15분마다 실행되는 하트비트의 누적 지연을 비교했다. 아래 시각은 한국 시간이다.

| 사건 | 목표 시각 | 실제 처리 시각 | 예약 처리 지연 | 하트비트 누적 지연 |
|---|---|---|---:|---:|
| 2026-09-09 | 16:00:00.038 | 17:06:06.356 | 3,966.318초 | 3,966.316초 |
| 2026-09-14 | 06:00:00.000 | 08:35:00.590 | 9,300.589초 | 9,300.621초 |

- 하트비트 누적 지연은 `마지막 기록 시각 - 첫 기록 시각 - (기록 수 - 1) × 900초`로 계산했다.
- 9월 9일은 예약 등록 이후 두 차례 시스템 잠자기·복귀가 기록됐다. 하트비트 20개의 첫 기록은 11:08:54.366, 마지막 기록은 17:00:00.682다.
- 9월 14일 예약은 9월 11일 16:00:37.661에 등록됐다. 이후 금요일 저녁부터 토요일 오전 사이 잠자기·복귀가 있었다. 하트비트 248개의 첫 기록은 9월 11일 16:01:10.062, 마지막 기록은 9월 14일 08:21:10.683이다.
- `pmset -g log`에서 실제 Sleep 진입부터 다음 Wake/DarkWake까지의 구간을 합산하면 각각 3,999초와 9,334초다. 전원 이벤트 시각으로 구한 구간 합과 타이머 시계의 정지량은 동일한 측정값은 아니지만, 지연 규모가 일치한다.
- 두 독립 타이머의 누적 지연이 각각 0.002초, 0.032초 차이로 맞는 점과 코드·플랫폼 동작을 근거로, 잠자기 중 정지하는 시간 기준과 복귀 시 재계산 누락을 이번 수정의 원인으로 판단한다.

근거 로그는 [events.jsonl](/Users/crobat/Library/Logs/ClaudeSessionWarmer/events.jsonl:4979)이다. 로그 회전 이후에도 찾을 수 있도록 `launch_id=C5B31D80-F37F-48EB-A2EB-28F97BCBB524`와 아래 sequence를 함께 사용한다.

| 사건 | 예약 등록 | 첫·마지막 하트비트 | 예약 처리 | 놓침 기록 |
|---|---:|---:|---:|---:|
| 9월 9일 | 788 | 789, 843 | 844 | 845 |
| 9월 14일 | 1470 | 1471, 1756 | 1757 | 1758 |

장애 당시 `timer.fired` 기록 위치는 타이머 콜백 내부의 MainActor 작업 안이었고 별도 콜백 진입 로그는 없었다. 이 로그만으로 콜백 자체와 MainActor 전달 지연을 구분할 수는 없었다. 이번 원인 판단은 위 독립 하트비트 비교와 전원 기록을 추가한 결과이며, App Nap이나 사용자 앱 활성화를 원인으로 전제하지 않는다.

## 2. 수정 후 동작 계약

1. 최초 예약, 실제 `resets_at` 기반 후속 예약, 실패 재시도 모두 실제 날짜·시각을 기준으로 설정한다.
2. 예약 시각 전에 잠자기에서 복귀하면 같은 목표 시각을 유지한다. 잠든 시간을 목표 시각에 더하지 않는다.
3. 예정 시각을 지나 복귀한 경우에는 기존 놓침·재시도 판정에 따라 처리한다. 놓친 창을 늦게 워밍하는 기능은 추가하지 않는다.
4. 하루 최대 3창, 실제 `resets_at` 우선, 실패·놓침 시 `targetAt + 5시간` fallback, 실행 요일·공휴일 규칙을 유지한다.
5. 이미 처리한 창은 다시 열지 않는다. 메시지 전송 가능 이력이 있으면 기존 중복 전송 방지를 유지한다.
6. 화면 잠금과 화면 꺼짐은 시스템 잠자기와 구분한다. 앱 종료 또는 시스템 잠자기 중 실행을 보장하거나 Mac을 강제로 깨우지 않는다.

### 시간 경계

`T`는 창의 목표 시각, `R`은 재시도 시각이다. 현재 코드의 다음 기준을 회귀 테스트로 보존한다.

| 조건 | 처리 |
|---|---|
| 콜백이 예정된 실행 시각보다 먼저 전달됨 | 서비스 호출 없이 같은 실행 시각으로 다시 예약 |
| 정상 예약의 첫 처리, `T ≤ 현재 ≤ T+5초` | 기존 정시 허용 범위에서 처리 |
| 첫 시도 이력이 없고 `현재 > T+5초` | 놓침 처리 |
| 이미 시작한 창의 재시도 | 기존 30초 재시도 간격과 `T+3분` 마감 유지 |
| `현재 > T+3분` | 추가 재시도 없이 종료. 기존 최초 실패가 있으면 실패 이유 보존 |

앱 재시작 때 이미 지난 첫 시각을 놓침으로 정리하는 현재 `reconcileMissedWindows` 동작도 유지한다. 5초와 3분은 서로 다른 목적의 기준이므로 합치거나 늘리지 않는다. 복귀 처리와 기존 타이머 콜백이 경합해도 같은 창에 대한 판정은 한 번만 반영한다.

## 3. 구현 작업

### TASK-01. 실제 시각 기준 단발 타이머로 변경

대상: [AppState.swift](/Users/crobat/dev/claude-session/Sources/ClaudeSessionWarmer/AppState.swift:282)

- `Timer`를 전용 직렬 큐의 `DispatchSourceTimer`로 교체한다.
- `schedule(wallDeadline:repeating:leeway:)`를 사용하고 `repeating`은 `.never`로 설정한다. 초기 `leeway`는 100ms로 둔다.
- 저장된 실행 `Date`를 `DispatchWallTime(timespec:)`로 변환한다. Unix 시각의 초와 소수부를 보존하고 나노초 범위를 올바르게 정규화한다.
- `deadline: .now() + delay` 방식은 사용하지 않는다. 이것도 잠자기 동안 멈추는 시간 기준이므로 이번 결함이 남는다.
- `ScheduledEvent.targetAt`은 창의 기준 시각, `ScheduledEvent.date`는 이번 실행·재시도 시각으로 계속 구분한다. 재시도 타이머는 `R`에 예약하되 마감은 원래 `T`로 판단한다.
- 콜백에서는 시각을 즉시 캡처한 뒤 MainActor로 전달한다. 상태 저장과 기존 워밍 처리는 현재 MainActor 경로를 재사용한다.
- 시스템이 콜백을 이르게 전달할 수 있으므로 처리 직전 `현재 < 이번 실행 시각`이면 서비스 호출 없이 다시 예약한다.

완료 조건: 스케줄링 경로에서 상대 uptime 기반 예약이 제거되고, 첫 예약·후속 예약·재시도가 동일한 실제 시각 기준을 사용한다.

### TASK-02. 복귀·시계 변경 시 재계산 및 중복 방지

대상: [DiagnosticLogger.swift의 LifecycleMonitor](/Users/crobat/dev/claude-session/Sources/ClaudeSessionWarmer/DiagnosticLogger.swift:193), [AppState.swift](/Users/crobat/dev/claude-session/Sources/ClaudeSessionWarmer/AppState.swift:260)

- 기존 `NSWorkspace.didWakeNotification` 관찰 경로를 통해 AppState에 재계산을 요청한다.
- 시스템 시각 변경은 `NSSystemClockDidChange` 알림으로 같은 경로에 연결한다. macOS 14에서 사용할 수 있는 기존 NotificationCenter API를 사용한다.
- 대기 중이면 이전 타이머를 취소하고 현재 `Date`, 설정, 저장된 일일 주기로 예약을 다시 계산한다. 미래의 정상 예약은 동일한 `targetAt`을 유지한다.
- 재시도를 기다리는 중이면 아직 유효한 `R`을 보존한다. 복귀 알림 때문에 30초 재시도가 즉시 실행되거나 마감이 연장되지 않도록 한다.
- 워밍·수동 작업이 실행 중이면 그 결과를 먼저 반영하고 재계산한다. 재계산 요청은 하나로 합쳐 작업 종료 경로에서 빠짐없이 처리한다. 완료 전 `reconcileMissedWindows`가 같은 창을 먼저 닫는 경합을 막는다.
- 예약 교체·취소 때 갱신하는 세대 번호 또는 식별자 하나를 둔다. MainActor 처리 직전 확인해 이미 큐에 들어온 이전 콜백을 무시한다. 날짜가 같아도 교체된 예약을 구별해야 한다.
- 이미 처리한 창의 콜백 무시, 타이머 취소, observer 해제, 앱 상태 해제 시 참조 정리를 검증한다.

완료 조건: 여러 번의 복귀·시계 변경·설정 저장이 발생해도 유효한 예약은 하나이며, 실행 중 작업이나 저장된 결과가 중복 처리되지 않는다.

### TASK-03. 지연 지점을 구분하는 로그

대상: AppState의 예약 경로와 기존 DiagnosticLogger

- `timer.armed`: 예약 식별자, 목표 시각, 이번 실행 시각, 시간 기준 `wall`, 설정 이유를 기록한다.
- `timer.callback`: 백그라운드 콜백 진입 즉시 시각을 캡처하고 기록한다.
- 기존 `timer.fired`: MainActor 처리 진입 기록으로 유지하되 콜백 시각과 전달 지연을 추가한다. 두 이벤트의 의미를 문서에 명시한다.
- `schedule.reconciled`: 복귀·시각 변경 이유와 재계산 전후 목표 시각을 기록한다.
- 같은 launch와 예약 식별자로 등록 → 콜백 → 처리 → 결과를 연결한다. 취소된 콜백은 무시된 이유를 남긴다.
- 로그 출력 시각과 실제 측정 시각이 혼동되지 않도록 측정값을 먼저 캡처한다. 기존 민감정보 제외·파일 권한·회전 규칙을 유지한다.

완료 조건: 다음 장애에서 타이머 전달 지연과 MainActor 처리 지연을 독립적으로 계산할 수 있다.

### TASK-04. 회귀 테스트와 실제 잠자기 검증

기존 `clock`, 가짜 서비스, 격리된 UserDefaults를 재사용한다. 실제 타이머의 등록·취소를 관찰하는 데 필요한 최소 주입 지점만 추가한다. 일반 스케줄러 프레임워크는 만들지 않는다.

| 검증 | 입력·상황 | 합격 조건 |
|---|---|---|
| 목표 유지 | 11시 등록 → 중간 잠자기 → 15시 복귀 → 16시 목표 | 목표가 16시 그대로이며 잠든 시간이 추가되지 않음 |
| 주말 대기 | 금요일 등록 → 여러 잠자기·복귀 → 월요일 06시 목표 | 같은 월요일 06시를 유지 |
| 경계 판정 | `T` 직전, `T+5초`, 그 직후, `T+3분`, 그 직후 | 조기 호출 없음, 기존 최초 시도·재시도 경계 보존 |
| 시각 변경 | 시계 전진으로 목표 통과, 후진으로 다시 미래가 됨 | 놓침 정책 적용 또는 미래 재예약, 처리한 창 재실행 없음 |
| 취소 경합 | 설정 저장·복귀로 교체된 이전 콜백을 나중에 전달 | 이전 콜백 서비스 호출 0회, 새 예약만 유효 |
| 실행 중 복귀 | 수동·자동 워밍 실행 중 재계산 요청 여러 번 | 실행은 하나, 결과 저장 후 재계산 한 번, 최초 실패 보존 |
| 재시도 복귀 | `R` 전에 복귀, `R` 이후 또는 `T+3분` 이후 복귀 | 미도래 `R` 유지, 유효 기간 내 기존 정책 적용, 마감 연장 없음 |
| 기존 정책 | 실제 리셋·fallback·최대 3창·휴일·재시작 | 기존 ScheduleEngine 및 통합 테스트 통과 |

기본 검증 명령은 프로젝트 루트에서 `swift test`, `swift build -c release`다. 자동 테스트는 OS 잠자기의 실제 타이머 동작을 증명하지 못하므로 아래 실기기 검증을 별도로 수행한다.

1. 실제 Claude 호출과 분리한 검증 프로그램으로 앱과 동일한 예약 코드의 기본 동작을 확인한다. 잠자기 없는 기본 검증과 실제 잠자기 검증은 구별한다.
2. 0.1.4 설치본을 평소대로 사용하며 2026-09-14 다음 예약의 콜백·처리·결과 저장을 확인한다.
3. 9월 15일 이후 첫 예약과 실제 `resets_at`에 따른 후속 예약을 확인한다. 등록만 확인하고 실행까지 통과했다고 판정하지 않는다.
4. 자연스러운 예약에서 사용량 확인 → 필요 시 워밍 → 결과 저장 → 다음 예약까지 연결한다. 이미 활성인 경우는 `already_active`로 기록하고 새 창 생성 성공과 구분한다.
5. 등록 이후 전체 구간의 잠자기·복귀 기록을 함께 확인한다. 자연스러운 잠자기가 없으면 해당 조건은 미관찰로 기록하며 인위적으로 잠들게 하지 않는다.
6. 지연이나 오류가 발생하면 잠자기 유무와 관계없이 콜백 전 지연, MainActor 전달 지연, 서비스 실패를 나누어 실제 로그로 조사한다.

수동 작업이나 사용자 입력과 경합하지 않는 정상 검증 조건에서 처리 시작은 `0 ≤ 현재-T ≤ 5초`여야 한다. 잠자기 누적 시간에 비례하는 지연은 0이어야 한다. 100ms leeway를 실행 시각의 절대 보장으로 해석하지 않는다.

사용자 지시에 따라 강제 잠자기 검증은 완료의 필수 게이트에서 제외한다. 무인 예약 결과는 이후 로그로 확인하며 수동 워밍으로 보완하지 않는다. 준비된 잠자기 검증 도구는 향후 필요할 때 사용할 수 있도록 보존한다.

### TASK-05. 문서와 배포 결과 정리

- 구현 후 기존 [개발 계획서](/Users/crobat/dev/claude-session/04_DEVELOPMENT_PLAN.md)의 예약 기술 설명과 [테스트 계획서](/Users/crobat/dev/claude-session/03_TEST_PLAN.md)에 확정된 변경만 반영한다.
- README에는 예약 시각 전 잠자기·복귀와 예약 시각 자체를 놓친 경우의 차이를 설명한다.
- 새 검증 결과에는 커밋, 앱 버전·빌드, launch, 예약 식별자, 잠자기·복귀 시각, 목표·콜백·처리 시각을 남긴다. 기존 9월 8일 검증 결과를 이번 수정의 증거로 사용하지 않는다.
- 배포 단계에서는 기존 서명 식별자를 유지하고 버전·빌드를 올린다. 기존 빌드 스크립트로 서명·공증·stapling을 검증하고, 실행 중 앱이 새 빌드인지 확인한다.
- 설치형 검증 전까지는 구현 완료와 실운영 검증 완료를 구분한다. 이전 설치본·설정·장애 로그는 복구 가능하게 보존한다.

## 4. 작업 범위와 순서

구현 순서는 `TASK-01 → TASK-02 → TASK-03 → TASK-04 → TASK-05`이며 서브에이전트를 사용하지 않는다. 필요하면 진단 로그를 타이머 변경과 함께 작성한다.

예약 수정의 핵심 파일은 AppState와 DiagnosticLogger, 기존 예약 관련 테스트다. ScheduleEngine은 계산 규칙을 재사용하고 실제 결함이 확인된 부분만 수정한다. Claude OAuth·Keychain·PTY 방식은 유지한다. 추가 요청된 버전 표시와 수동 업데이트 기능은 08 문서 범위로 포함한다. 별도 서버·상주 helper·주기적 폴링·상시 App Nap 방지·시스템 전원 설정 변경은 추가하지 않는다. Sparkle에 포함된 설치 도구는 업데이트 시에만 사용한다.

구현 시작 시 작업 트리를 다시 확인하고 `codex/` 접두사의 작업 브랜치를 사용한다. 계획 작성 시점에 이미 수정된 [05_UNATTENDED_VALIDATION.md](/Users/crobat/dev/claude-session/05_UNATTENDED_VALIDATION.md)는 기존 변경으로 보존한다.

## 5. 완료 기준

- [x] 실제 시각 기준 단발 예약과 복귀·시각 변경 재계산 구현
- [x] 최초 5초·재시도 3분 경계, 원래 `targetAt`, 실패 이유와 중복 방지 보존
- [x] 예약 취소 경합 및 실행 중 복귀 회귀 테스트 통과
- [x] `swift test`와 최종 release 빌드 통과
- [x] 서명·공증된 0.1.4 빌드 6 설치 및 실행 확인
- [ ] 오늘 설치형 빌드의 실제 후속 예약과 결과 저장 확인
- [ ] 내일 이후 첫 예약과 실제 리셋에 따른 후속 예약·결과 저장 확인
- [x] 추가 기능을 포함한 최신 빌드의 공증·최종 배포 패키지 검증
- [x] 근거 로그·빌드 식별·문서 갱신 및 미검증 항목 명시

진행 증거: [07_SCHEDULER_FIX_VALIDATION.md](/Users/crobat/dev/claude-session/07_SCHEDULER_FIX_VALIDATION.md). 오늘·내일 이후 실사용 예약 게이트가 남아 있으므로 전체 완료 상태는 아니다.

## 6. 플랫폼 근거

- [Apple: Dispatch Sources](https://developer.apple.com/library/archive/documentation/General/Conceptual/ConcurrencyProgrammingGuide/GCDWorkQueues/GCDWorkQueues.html) — 기본 시계는 시스템 잠자기 중 진행하지 않으며, wall time은 실제 시각을 추적한다.
- [Apple: schedule(wallDeadline:repeating:leeway:)](https://developer.apple.com/documentation/dispatch/dispatchsourcetimer/schedule%28walldeadline%3Arepeating%3Aleeway%3A%29-7c4d7) — 실제 시각 예약, 단발 설정, 허용 지연과 조기 전달 가능성.
- [Apple: NSSystemClockDidChange](https://developer.apple.com/documentation/foundation/nsnotification/name-swift.struct/nssystemclockdidchange) — 시스템 시각 변경 알림.
- [Apple 공개 CoreFoundation 구현](https://github.com/apple-oss-distributions/CF/blob/main/CFRunLoop.c) — `CFRunLoopTimerCreate`에서 목표 날짜를 `mach_absolute_time` 기준의 타이머 값으로 변환하는 구현 참고. 현재 설치 OS와 소스 버전이 동일하다는 뜻은 아니다.
